# Pi

- Default: `opencode-go/kimi-k2.7-code` — local via oMLX when offline
- Tools: bash + `gh`/`rg`/`git`; web via `web_search`/`fetch_content`
- Permissions: OpenCode-style gates (ask bash/write; deny sudo/rm -rf/destroy)
- Prefer small diffs; no drive-by refactors
