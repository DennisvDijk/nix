# Pi (lean)

Packages: web-access, permissions, opencode-go-provider
Default: opencode-go/kimi-k2.7-code

```bash
pi
```

Auth: opencode-go key via ~/.pi/agent/auth.json → opencode auth.json
Models: /model or Ctrl+P (omlx + go favorites)
Optional web key: ~/.pi/web-search.json
