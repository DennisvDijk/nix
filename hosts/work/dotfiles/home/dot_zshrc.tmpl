# dot_zshrc.tmpl
# Work-specific zsh config managed by chezmoi
# This file is templated — chezmoi renders it to ~/.zshrc
#
# Add work-specific shell config here that doesn't belong in the nix home-manager config:
# - Company proxy settings
# - Internal tool aliases
# - Project-specific shell functions
#
# Example:
# export PROXY_URL="http://proxy.internal:3128"
# alias vpn='openconnect vpn.enexis.nl'
